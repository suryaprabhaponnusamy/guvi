#include<stdio.h>
int main()
{
int n,min,hours;
  printf("enter the minutes: ");
  scanf("%d",&min);
  {
    hour=min/60;
 n=min%60;
  }
    printf("the hour and minutes are %d%d",hour,n);
    return 0;
  }
